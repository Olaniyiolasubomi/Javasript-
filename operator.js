//  OPERATOR IN FOUR GROUPS:
// 1. Assignment (=, +=, -=, /=, *=, **=, %=)

var studentName="Praise" 
// 2. Maths (+, -, /, *, %, **,)
var num1=5
var num2=2
var add= num1+num2
console.log(`addition of ${num1}+${num2}=${add}`)

var subtract= num1-num2
console.log(`subtraction of ${num1}-${num2}=${subtract}`)

var divide= num1/num2
console.log(`divition of ${num1}/${num2}=${divide}`)

var multiply= num1*num2
console.log(`multiplication of ${num1}*${num2}=${multiply}`)

var exponent= num1**num2
console.log(`exponential of ${num1}**${num2}=${exponent}`)

var modulus= num1%num2
console.log(`modulus of ${num1}%${num2}=${modulus}`)
// 3. Comparison (==, !=, ===, !==, <, <=, >, >=)

// (== is equal to, != is not equal to, === is ident
// ical, !== is not identical, < is less than, <= is 
// less than or equal to, > is greater than, >= is 
// greater than or equals)

var age=20
console.log(`is 20 equal to 20: ${age==20}`)
console.log(`is 20 greater than 20: ${age>20}`)
console.log(`is 20 less than 20: ${age<20}`)
console.log(`is 20 greater than or equal to 20: ${age>=20}`)
console.log(`is 20 less than or equal to 20: ${age<=20}`)
console.log(`is 20 not equal to 20: ${age!=20}`)
// 4. Logical (&, ||fr)
  
// % is not percentage, but it is known as modulus